
import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
    colors: {
    },
    fonts : {
        heading: `'dDicapslock', sans-serif`,
        body: `'dDicapslock', sans-serif`,
    },
    styles: {
        global: {
            body: {
                bg: '#281A32'
            },
        }
    }
})